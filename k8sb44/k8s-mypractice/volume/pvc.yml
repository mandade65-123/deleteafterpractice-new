apiVersion: v1
kind:  PersistentVolumeClaim
metadata:
    name: pv-claim
    labels:
        app: pv-claim
spec:
    storageClassName: standard
    volumeMode: filesystem
    accessMode:
      - ReadWriteOnce
    resources:
        requests:
          storage: 10Gi
          

